import React from 'react';

const SvgIconBoxUnchecked = props => (
  <svg width={16} height={16} fill="none" {...props}>
    <rect x={0.5} y={0.5} width={15} height={15} rx={2.5} stroke="#B4BFC9" />
  </svg>
);

export default SvgIconBoxUnchecked;
