import React from 'react';
import { storiesOf } from '@storybook/react';
import CheckboxesGroup from './CheckboxesGroup';

storiesOf('Component groups', module).add('CheckboxesGroup', () => (
  <CheckboxesGroup />
));
