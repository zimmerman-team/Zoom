import React from 'react';
import { storiesOf } from '@storybook/react';
import CustomYearSelector from './CustomYearSelector';

storiesOf('Components', module).add('CustomYearSelector', () => (
  <CustomYearSelector />
));
