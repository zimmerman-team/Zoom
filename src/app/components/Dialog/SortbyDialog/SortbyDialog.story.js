import React from 'react';
import { storiesOf } from '@storybook/react';
import SortbyDialog from './SortbyDialog';

storiesOf('Components', module).add('SortbyDialog', () => (
  <SortbyDialog open />
));
