import React from 'react';
import { storiesOf } from '@storybook/react';
import DataExplorePanel from './DataExplorePanel';

storiesOf('Components', module).add('DataExplorePane', () => (
  <DataExplorePanel />
));
