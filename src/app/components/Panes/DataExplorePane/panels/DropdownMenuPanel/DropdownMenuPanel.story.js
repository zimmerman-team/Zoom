import React from 'react';
import { storiesOf } from '@storybook/react';
import DropdownMenuPanel from './DropdownMenuPanel';

storiesOf('Components', module).add('DropdownMenuPanel', () => (
  <DropdownMenuPanel />
));
