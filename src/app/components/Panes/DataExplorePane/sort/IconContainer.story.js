import React from 'react';
import { storiesOf } from '@storybook/react';
import IconContainer from 'components/Panes/DataExplorePane/sort/IconContainer';

storiesOf('Components', module).add('IconContainer', () => <IconContainer />);
