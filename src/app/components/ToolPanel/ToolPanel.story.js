import React from 'react';
import { storiesOf } from '@storybook/react';
import ToolPanel from './ToolPanel';

storiesOf('Datamapper|Components/Tool Panel/', module).add('ToolPanel', () => (
  <ToolPanel />
));
