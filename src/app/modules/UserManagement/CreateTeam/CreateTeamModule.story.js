import React from 'react';
import { storiesOf } from '@storybook/react';
import CreateTeamModule from './CreateTeamModule';

storiesOf('User Management|Modules/', module).add('CreateTeamModule', () => (
  <CreateTeamModule />
));
