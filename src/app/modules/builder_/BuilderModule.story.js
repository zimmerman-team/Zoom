import React from 'react';
import { storiesOf } from '@storybook/react';
import BuilderModule from './BuilderModule';

storiesOf('Builder|Module', module).add('BuilderModule', () => (
  <BuilderModule />
));
