import React from 'react';
import { storiesOf } from '@storybook/react';
import BuilderSidebar from './BuilderSidebar';

storiesOf('Builder|Components', module).add('BuilderSidebar', () => (
  <BuilderSidebar />
));
