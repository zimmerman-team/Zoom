import React from 'react';
import { storiesOf } from '@storybook/react';
import FindReplace from './FindReplace';

storiesOf('Datamapper|Fragments/Error Step/Components', module).add(
  'FindReplace',
  () => <FindReplace open />
);
