import React from 'react';
import { storiesOf } from '@storybook/react';
import ToolsModule from './ToolsModule';

storiesOf('Module: tools', module).add('0. ToolCoordinator', () => (
  <ToolsModule />
));
