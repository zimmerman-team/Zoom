import React from 'react';
import { storiesOf } from '@storybook/react';
import VizSidebar from './VizSidebar';

storiesOf('Visualizer|Components', module).add('VizSidebar', () => (
  <VizSidebar />
));
